{
  "name": "rabbithole",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio rabbithole-credentials",
    "create-local": "graph create --node http://localhost:8020/ anudit/rabbithole",
    "remove-local": "graph remove --node http://localhost:8020/ anudit/rabbithole",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 anudit/rabbithole"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.4",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
